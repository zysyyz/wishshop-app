class UserState {
  // UserState({});

  static UserState fromJson(dynamic json) {
    if (json == null) {
      return UserState();
    }
    return UserState(
    );
  }

  Map<String, dynamic> toJson() {
    return {
    };
  }
}
